services:
  relay:
    image: node:22-alpine
    working_dir: /app
    volumes:
      - .:/app
    command: sh -c "npm install && node server.js"
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    restart: unless-stopped
